============
Installation
============

At the command line::

    $ easy_install pyremotevbox

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pyremotevbox
    $ pip install pyremotevbox
